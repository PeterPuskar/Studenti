using System;

namespace Studenti
{
    class Program
    {
        static void Main(string[] args)
        {
            StudentsDB databaza = new StudentsDB();
            databaza.VytvorDatabazu();
            databaza.ZoradenePole
            Console.ReadKey();

            foreach(Studenti) in ;
        }

        bool bylo;
        if(trideno) bylo = true;
        
        bylo = (trideno) true : false
            int delka = (databaze.seznamStudentu.Count ==4) ? 4 : 1
            /* inlinovka teda namiesto if sa priraduje podmienka a true a false
    }
}
